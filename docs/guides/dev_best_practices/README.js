Ext.data.JsonP.dev_best_practices({"guide":"<h1 id='dev_best_practices-section-ckeditor-best-practices'>CKEditor Best Practices</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_best_practices-section-installation'>Installation</a></li>\n<li><a href='#!/guide/dev_best_practices-section-configuration'>Configuration</a></li>\n<li><a href='#!/guide/dev_best_practices-section-integration'>Integration</a></li>\n</ol>\n</div>\n\n<p>This article contains a list of some best practices that we recommend when implementing CKEditor. Check the links for more information about particular items.</p>\n\n<h2 id='dev_best_practices-section-installation'>Installation</h2>\n\n<h3 id='dev_best_practices-section-upgrade-regularly'>Upgrade regularly</h3>\n\n<p>CKEditor should be <a href=\"#!/guide/dev_upgrade\">upgraded frequently</a> as bug fixes and new features, including support for new browser versions, are not backported.</p>\n\n<p>If you modified any distribution files to add your custom configuration, pay special attention to not overwrite your content (including <code>config.js</code>, <code>contents.css</code>, <code>styles.js</code> and custom template files) when upgrading.</p>\n\n<h3 id='dev_best_practices-section-use-minified-versions'>Use minified versions</h3>\n\n<p>You should always use minified CKEditor versions (<a href=\"http://ckeditor.com/download\">official releases</a>, <a href=\"http://ckeditor.com/builder\">optimized</a> <a href=\"#!/guide/dev_build\">builds</a>) in production environments. The <a href=\"#!/guide/dev_source\">development, source code version</a> is only suitable for tests and development.</p>\n\n<p>Additionally, it is good to enable <code>gzip</code> compression in your server settings.</p>\n\n<h3 id='dev_best_practices-section-do-not-be-afraid-of-custom-builds'>Do not be afraid of custom builds</h3>\n\n<p>If you want to skip some editor features, <a href=\"http://ckeditor.com/builder\">customize your build with CKBuilder</a> and remove unneeded functionality.</p>\n\n<p>It is a bad practice to download the Full package and then <a href=\"#!/api/CKEDITOR.config-cfg-removePlugins\">remove plugins</a> or <a href=\"#!/api/CKEDITOR.config-cfg-removeButtons\">buttons</a> in your configuration. You will only be loading unnecessary stuff without any good reason.</p>\n\n<h3 id='dev_best_practices-section-use-ckbuilder-to-add-plugins'>Use CKBuilder to add plugins</h3>\n\n<p>If you want to install additional plugins, <a href=\"#!/guide/dev_plugins\">use CKBuilder instead of adding them manually</a>.</p>\n\n<p>This will reduce the risk of omitting plugin dependencies and in the created optimized build all plugins will be merged into one file, which will reduce time needed to load them.</p>\n\n<h2 id='dev_best_practices-section-configuration'>Configuration</h2>\n\n<h3 id='dev_best_practices-section-configure-content-filtering'>Configure content filtering</h3>\n\n<p><a href=\"#!/guide/dev_advanced_content_filter\">Advanced Content Filter</a> is a cool feature which assures that the content inserted by the user blends in with your website in the best possible way. If you have any issues with it, do not turn it off, but instead configure it to match your requirements.</p>\n\n<p>For example, to enable <code>&lt;iframe&gt;</code> and <code>&lt;style&gt;</code> elements, allow two additional attributes (<code>id</code> and <code>rel</code> given in square brackets) for all (<code>*</code> is a wildcard) already enabled elements, and allow usage of any class names (see <code>(*)</code>) for them, use the following code:</p>\n\n<pre><code>config.extraAllowedContent = 'iframe style;*[id,rel](*)';\n</code></pre>\n\n<h3 id='dev_best_practices-section-pasting-as-plain-text-%26ne%3B-content-filtering'>Pasting as plain text &ne; content filtering</h3>\n\n<p>Setting <a href=\"#!/api/CKEDITOR.config-cfg-forcePasteAsPlainText\" rel=\"CKEDITOR.config-cfg-forcePasteAsPlainText\" class=\"docClass\">CKEDITOR.config.forcePasteAsPlainText</a> to <code>true</code> will not replace the content filtering features that <a href=\"#!/guide/dev_advanced_content_filter\">Advanced Content Filter</a> can provide. It will however make it impossible for users to paste items such as tables or lists, where structure matters most.</p>\n\n<h3 id='dev_best_practices-section-use-enter-mode-p'>Use Enter mode P</h3>\n\n<p>Changing the <a href=\"#!/api/CKEDITOR.config-cfg-enterMode\">Enter Mode</a> setting to <code>BR</code> or <code>DIV</code> is not recommended. The default <code><a href=\"#!/api/CKEDITOR-property-ENTER_P\" rel=\"CKEDITOR-property-ENTER_P\" class=\"docClass\">CKEDITOR.ENTER_P</a></code> mode is fully supported by all editor features and plugins and is also the most correct one in terms of best practices for creating web content.</p>\n\n<p>If you do it to control paragraph spacing, you should use stylesheets instead. Edit the <code>contents.css</code> file and set up a suitable <code>margin</code> value for <code>&lt;p&gt;</code> elements, for example:</p>\n\n<pre><code>p { margin: 0; }\n</code></pre>\n\n<h3 id='dev_best_practices-section-configure-styles-drop-down'>Configure Styles drop-down</h3>\n\n<p>If you are using a CKEditor build which includes the Styles drop-down (like Standard or Full), take a few minutes to <a href=\"#!/guide/dev_styles\">configure it</a> after you install the editor.</p>\n\n<p>The default styles included in the <code>styles.js</code> file are just examples which will not work without the matching <code>contents.css</code> file. They will also not work in the inline editor which uses page stylesheets instead of the <code>contents.css</code> file.</p>\n\n<h2 id='dev_best_practices-section-integration'>Integration</h2>\n\n<h3 id='dev_best_practices-section-input-clean-html-code'>Input clean HTML code</h3>\n\n<p>You should only ever input clean, standards-compliant HTML code into CKEditor. <a href=\"#!/guide/dev_basics-section-how-ckeditor-works\">Incorrect markup will be modified by CKEditor</a> which may lead to unexpected results.</p>\n\n<h3 id='dev_best_practices-section-use-utf-8'>Use UTF-8</h3>\n\n<p>To avoid problems with character encoding, use UTF-8 for your websites and your database. Just set the <code>&lt;meta&gt;</code> element for your pages to:</p>\n\n<pre><code>&lt;meta http-equiv=\"Content-Type\" content=\"text/html; charset=utf-8\"&gt;\n</code></pre>\n\n<p>or, if you are using the HTML5 <code>DOCTYPE</code>, to:</p>\n\n<pre><code>&lt;meta charset=\"utf-8\"&gt;\n</code></pre>\n\n<h3 id='dev_best_practices-section-use-ckeditor-for-what-it-was-made-for'>Use CKEditor for what it was made for</h3>\n\n<p>Last but not least, <a href=\"#!/guide/dev_basics-section-what-ckeditor-is\">use CKEditor for what it was designed for</a>.</p>\n","title":"Best Practices","meta_description":"Recommended best practices for CKEditor.","meta_keywords":"ckeditor, editor, wysiwyg, basics, basic, best, practice, practices, recommendation, recommendations, standards, solution, solutions, installation, configuration, integration"});
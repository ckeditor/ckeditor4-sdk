Ext.data.JsonP.dev_inline({"guide":"<h1 id='dev_inline-section-inline-editing'>Inline Editing</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_inline-section-enabling-inline-editing'>Enabling Inline Editing</a></li>\n<li><a href='#!/guide/dev_inline-section-retrieving-the-editor-data'>Retrieving the Editor Data</a></li>\n</ol>\n</div>\n\n<p>Inline Editing is a new technology introduced in CKEditor 4 that allows you to <strong>select any editable element on the page and edit it in-place</strong>. As a result, the editor can be used to edit pages that look just like the final page.</p>\n\n<p>It is a total WYSIWYG experience, because not only the edited content looks like the final outcome, but also the page and the context where the content is placed is the real one.</p>\n\n<p>To try it out, see the <a href=\"http://ckeditor.com/demo#inline\">demo on the official CKEditor site</a>.</p>\n\n<p><img src=\"guides/dev_ckeditor_js_load/inline_example.png\" alt=\"Inline editor example\" width=\"585\" height=\"294\"></p>\n\n<h2 id='dev_inline-section-enabling-inline-editing'>Enabling Inline Editing</h2>\n\n<p>Inline Editing is enabled directly on HTML elements through the HTML5 <code>contenteditable</code> attribute.</p>\n\n<p>Suppose, for example, that you want to make a <code>&lt;div&gt;</code> element editable. In order to achieve this, it is enough to do the following:</p>\n\n<pre><code>&lt;div id=\"editable\" contenteditable=\"true\"&gt;\n    &lt;h1&gt;Inline Editing in Action!&lt;/h1&gt;\n    &lt;p&gt;The \"div\" element that contains this text is now editable.\n&lt;/div&gt;\n</code></pre>\n\n<p>It is also possible to enable inline editing with explicit code, by calling the <a href=\"#!/api/CKEDITOR-method-inline\" rel=\"CKEDITOR-method-inline\" class=\"docClass\">CKEDITOR.inline</a> method for the element that needs to have it enabled. Note that in this case you need to turn off automatic editor creation first by setting the <a href=\"#!/api/CKEDITOR-cfg-disableAutoInline\" rel=\"CKEDITOR-cfg-disableAutoInline\" class=\"docClass\">CKEDITOR.disableAutoInline</a> option to <code>true</code>:</p>\n\n<pre><code>&lt;div id=\"editable\" contenteditable=\"true\"&gt;\n    &lt;h1&gt;Inline Editing in Action!&lt;/h1&gt;\n    &lt;p&gt;The \"div\" element that contains this text is now editable.\n&lt;/div&gt;\n&lt;script&gt;\n    // Turn off automatic editor creation first.\n    <a href=\"#!/api/CKEDITOR-cfg-disableAutoInline\" rel=\"CKEDITOR-cfg-disableAutoInline\" class=\"docClass\">CKEDITOR.disableAutoInline</a> = true;\n    <a href=\"#!/api/CKEDITOR-method-inline\" rel=\"CKEDITOR-method-inline\" class=\"docClass\">CKEDITOR.inline</a>( 'editable' );\n&lt;/script&gt;\n</code></pre>\n\n<p>When you click inside the contents of this <code>&lt;div&gt;</code> element, the CKEditor toolbar will appear.</p>\n\n<p class=\"tip\">\n    The list of elements that support inline editing is available in the <a href=\"#!/api/CKEDITOR.dtd-property-S-editable\" rel=\"CKEDITOR.dtd-property-S-editable\" class=\"docClass\">CKEDITOR.dtd.$editable</a> property.\n</p>\n\n\n<h3 id='dev_inline-section-inline-editing-for-textarea'>Inline Editing for Textarea</h3>\n\n<p>Since CKEditor 4.2 you can also turn <code>&lt;textarea&gt;</code> elements into inline editors. When you call the <a href=\"#!/api/CKEDITOR-method-inline\" rel=\"CKEDITOR-method-inline\" class=\"docClass\">CKEDITOR.inline</a> method on a <code>&lt;textarea&gt;</code>, an additional <code>&lt;div&gt;</code> element with inline editing enabled will replace the original <code>&lt;textarea&gt;</code>.</p>\n\n<h2 id='dev_inline-section-retrieving-the-editor-data'>Retrieving the Editor Data</h2>\n\n<p>Unlike in <a href=\"#!/guide/dev_framed\">classic editing</a>, the data edited with CKEditor is not placed inside a <code>&lt;textarea&gt;</code> when using inline editing. It is present directly in the page DOM structure instead. It is thus the job of your application to retrieve the data and manipulate it for saving.</p>\n\n<p>To retrieve the editor data, call the <a href=\"#!/api/CKEDITOR.editor-method-getData\" rel=\"CKEDITOR.editor-method-getData\" class=\"docClass\">CKEDITOR.editor.getData</a> method of the editor instance. For the examples above, this would look like the following:</p>\n\n<pre><code>&lt;script&gt;\n    var data = CKEDITOR.instances.editable.getData();\n\n    // Your code to save \"data\", usually through Ajax.\n&lt;/script&gt;\n</code></pre>\n\n<p class=\"tip\">\n    If you do not save your data with a library that already encodes it by using the JavaScript <code>encodeURIComponent</code> method, but do it manually instead, you will have to remember to use <code>encodeURIComponent</code> to properly encode the data that is being sent.\n</p>\n\n\n<p>Note that the ID of the original <code>&lt;div&gt;</code> was passed to the <a href=\"#!/api/CKEDITOR-property-instances\" rel=\"CKEDITOR-property-instances\" class=\"docClass\">CKEDITOR.instances</a> object to make it possible to retrieve the editor instance.</p>\n","title":"Inline Editing","meta_description":"What inline editing is and how to enable it.","meta_keywords":"ckeditor, editor, wysiwyg, inline, inplace, in-place"});
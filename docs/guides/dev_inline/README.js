Ext.data.JsonP.dev_inline({"guide":"<h1 id='dev_inline-section-inline-editing'>Inline Editing</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_inline-section-enabling-inline-editing'>Enabling Inline Editing</a></li>\n<li><a href='#!/guide/dev_inline-section-retrieving-the-editor-data'>Retrieving the Editor Data</a></li>\n</ol>\n</div>\n\n<p>Inline Editing is a new technology designed to make it possible to edit pages that look just like the final page. It is a total WYSIWYG experience, because not only the edited content will look like the final outcome, but also the page and the context where the content is placed will be the real one.</p>\n\n<h2 id='dev_inline-section-enabling-inline-editing'>Enabling Inline Editing</h2>\n\n<p>Inline Editing is enabled directly on HTML elements through the HTML5 <code>contenteditable</code> attribute.</p>\n\n<p>Suppose, for example, that you want to make a <code>&lt;div&gt;</code> element editable. In order to achieve this, it is enough to do so:</p>\n\n<pre><code>&lt;div id=\"editable\" contenteditable=\"true\"&gt;\n    &lt;h1&gt;Inline Editing in Action!&lt;/h1&gt;\n    &lt;p&gt;The div element that contains this text is now editable.\n&lt;/div&gt;\n</code></pre>\n\n<p>It is also possible to enable editing with explicit code, by calling the <a href=\"#!/api/CKEDITOR-method-inline\" rel=\"CKEDITOR-method-inline\" class=\"docClass\">CKEDITOR.inline</a> method:</p>\n\n<pre><code>&lt;div id=\"editable\" contenteditable=\"true\"&gt;\n    &lt;h1&gt;Inline Editing in Action!&lt;/h1&gt;\n    &lt;p&gt;The div element that contains this text is now editable.\n&lt;/div&gt;\n&lt;script&gt;\n    // Turn off automatic editor creation first.\n    <a href=\"#!/api/CKEDITOR-cfg-disableAutoInline\" rel=\"CKEDITOR-cfg-disableAutoInline\" class=\"docClass\">CKEDITOR.disableAutoInline</a> = true;\n    <a href=\"#!/api/CKEDITOR-method-inline\" rel=\"CKEDITOR-method-inline\" class=\"docClass\">CKEDITOR.inline</a>( 'editable' );\n&lt;/script&gt;\n</code></pre>\n\n<p>When you click inside the contents of this <code>&lt;div&gt;</code> element, the CKEditor toolbar will appear.</p>\n\n<h2 id='dev_inline-section-retrieving-the-editor-data'>Retrieving the Editor Data</h2>\n\n<p>Unlike in <a href=\"#!/guide/dev_framed\">Framed Editing</a>, the data edited with CKEditor is not placed inside a <code>&lt;textarea&gt;</code> when using Inline Editing. It is instead present directly in the page DOM. Because of this, it is the job of your application to retrieve the data and manipulate it for saving.</p>\n\n<p>To retrieve the editor data, simply call the <a href=\"#!/api/CKEDITOR.editor-method-getData\" rel=\"CKEDITOR.editor-method-getData\" class=\"docClass\">CKEDITOR.editor.getData</a> method of the editor instance. For the above examples, this would look like the following:</p>\n\n<pre><code>&lt;script&gt;\n    var data = CKEDITOR.instances.editable.getData();\n\n    // Your code to save \"data\", usually through Ajax.\n&lt;/script&gt;\n</code></pre>\n\n<p class=\"tip\">\n    If you do not save your data with a library that already encodes it by using <code>encodeURIComponent</code>, but do it manually instead, you will have to remember to use <code>encodeURIComponent</code> to properly encode the data that is being sent.\n</p>\n\n\n<p>Note that the ID of the original <code>&lt;div&gt;</code> was passed to the <a href=\"#!/api/CKEDITOR-property-instances\" rel=\"CKEDITOR-property-instances\" class=\"docClass\">CKEDITOR.instances</a> object to make it possible to retrieve the editor instance.</p>\n","title":"Inline Editing","meta_description":null,"meta_keywords":null});
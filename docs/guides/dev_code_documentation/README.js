Ext.data.JsonP.dev_code_documentation({"guide":"<h1 id='dev_code_documentation-section-code-documentation-standards'>Code Documentation Standards</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_code_documentation-section-useful-links'>Useful Links</a></li>\n<li><a href='#!/guide/dev_code_documentation-section-jsduck-vs-jsdoc-%26mdash%3B-important-differences'>JSDuck vs JSDoc &mdash; Important Differences</a></li>\n<li><a href='#!/guide/dev_code_documentation-section-documentation-formats'>Documentation Formats</a></li>\n<li><a href='#!/guide/dev_code_documentation-section-miscellaneous-rules'>Miscellaneous Rules</a></li>\n</ol>\n</div>\n\n<p>Starting from version 4 CKEditor uses a customized version of <a href=\"https://github.com/ckeditor/jsduck\">JSDuck</a> as code documentation generator; previously <a href=\"http://code.google.com/p/jsdoc-toolkit/\">JSDoc</a> was used.</p>\n\n<p>JSDuck's comment format differs from JSDoc's and both generators have different feature lists. Thus, with CKEditor 4 release, the entire source code documentation was reformatted in a new, consistent way.</p>\n\n<p>The resulting CKEditor API documentation is always available at <a href=\"http://docs.ckeditor.com/#!/api\">http://docs.ckeditor.com/#!/api</a>.</p>\n\n<h2 id='dev_code_documentation-section-useful-links'>Useful Links</h2>\n\n<ul>\n<li><a href=\"https://github.com/ckeditor/jsduck\">CKEditor JSDuck customization</a></li>\n<li><a href=\"https://github.com/senchalabs/jsduck\">JSDuck GitHub page</a></li>\n<li><a href=\"https://github.com/senchalabs/jsduck/wiki/Guide\">JSDuck Wiki</a></li>\n<li><a href=\"https://github.com/senchalabs/jsduck/wiki/@class\">Tags list</a></li>\n<li><a href=\"http://daringfireball.net/projects/markdown/basics\">Markdown format - basics</a></li>\n<li><a href=\"http://docs.ckeditor.com/#!/api\">CKEditor API documentation</a></li>\n</ul>\n\n\n<h2 id='dev_code_documentation-section-jsduck-vs-jsdoc-%26mdash%3B-important-differences'>JSDuck vs JSDoc &mdash; Important Differences</h2>\n\n<ul>\n<li><strong>JSDuck supports Markdown</strong>. HTML entities may still be used, but try to avoid them in favor of Markdown. Note that HTML in code samples (indented blocks) and <code>pre-formatted</code> will be encoded.</li>\n<li>JSDuck does not accept the following tags: <code>@namespace</code>, <code>@name</code>, <code>@constant</code>, <code>@augments</code>, <code>@field</code>, <code>@type</code> (deprecated), <code>@default</code> and more (only those that CKEditor 3 was using are listed).</li>\n<li>JSDuck accepts some new tags: <code>@cfg</code>, <code>@member</code>, <code>@chainable</code>, <code>@inherits</code>, <code>@method</code>, <code>@mixins</code>, <code>@readonly</code>, <code>@singleton</code>, and <a href=\"https://github.com/senchalabs/jsduck/wiki/@class\">more</a>.</li>\n<li>Some common tags have different format in JSDuck (e.g. <code>@example</code> creates live examples, standard code samples are just indented).</li>\n<li>JSDuck does not parse code searching for classes and properties. It will therefore only find those API elements which have at least their preceding <code>/** */</code> doc comments.</li>\n<li>JSDuck recognizes API element names (methods, classes, events, configuration variables, properties, etc.), method definitions (with argument lists), and properties (their type and default value, too, if possible). Thus, in some cases this information does not have to be specified.</li>\n<li>Classes' definitions can be opened multiple times &mdash; it is useful when class methods and properties are defined in more than one file or place in the code.</li>\n<li>There is no list of files in JSDuck, so old <code>@license</code> and <code>@fileOverview</code> tags are kept for other purposes (and thanks to <a href=\"https://github.com/senchalabs/jsduck/wiki/Custom-tags\">custom tags</a> they work in JSDuck like <code>@ignore</code>).</li>\n<li>There are no namespaces in JSDuck, so the packages tree is auto-generated based on the classes tree and <code>@member</code> tags.</li>\n<li>All properties, events, etc. defined under the class definition will be assigned to this class, so there is no need to specify <code>@member</code>.</li>\n</ul>\n\n\n<h2 id='dev_code_documentation-section-documentation-formats'>Documentation Formats</h2>\n\n<h3 id='dev_code_documentation-section-file-header'>File Header</h3>\n\n<p>The <code>@license</code> and <code>@fileOverview</code> tags are legacy comments that will not be parsed by JSDuck.</p>\n\n<pre><code>/**\n * @license Copyright (c) 2003-2014, CKSource - Frederico Knabben. All rights reserved.\n * For licensing, see LICENSE.md or http://ckeditor.com/license\n */\n\n/**\n * @fileOverview Defines the <a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a> class that represents an\n * editor instance.\n */\n</code></pre>\n\n<p><strong>Note:</strong> Since <code>@fileOverview</code> comments are ignored, they have not been reformatted, so they may still contain the old JSDoc format.</p>\n\n<h3 id='dev_code_documentation-section-class'>Class</h3>\n\n<p>This is an example of a class definition. It contains so many tags to show their correct order.</p>\n\n<pre><code>/**\n * Represents an editor instance. This constructor should be rarely\n * used, in favor of the <a href=\"#!/api/CKEDITOR\" rel=\"CKEDITOR\" class=\"docClass\">CKEDITOR</a> editor creation functions.\n *\n *      var editor = new <a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a>();\n *      editor.setSomething( name, {\n *          value: 1\n *      } );\n *\n * @since 3.0\n * @private\n * @class <a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a>\n * @extends CKEDITOR.parent\n * @mixins <a href=\"#!/api/CKEDITOR.event\" rel=\"CKEDITOR.event\" class=\"docClass\">CKEDITOR.event</a>\n * @mixins CKEDITOR.whatever\n * @constructor Creates an editor class instance.\n * @param {Object} [instanceConfig] Configuration values for this specific instance.\n * @param {Number} [mode=CKEDITOR.SOURCE_MODE] The element creation mode to be used by this editor.\n *\n * Possible values are:\n *\n * * `CKEDITOR.SOURCE_MODE` - description 1,\n * * `CKEDITOR.WYSIWYG_MODE` - description 2 long long long\n *      long long long long long,\n * * `CKEDITOR.ANOTHER_MODE` - description 3.\n *\n * @param {<a href=\"#!/api/CKEDITOR.dom.element\" rel=\"CKEDITOR.dom.element\" class=\"docClass\">CKEDITOR.dom.element</a>} [element] The DOM element upon which this editor\n * will be created.\n * @see <a href=\"#!/api/CKEDITOR.editable\" rel=\"CKEDITOR.editable\" class=\"docClass\">CKEDITOR.editable</a>\n * @see CKEDITOR.cofing#someVar\n */\n<a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a> = function( ) {\n// ...\n</code></pre>\n\n<p>A minimal class documentation:</p>\n\n<pre><code>/**\n * Represents an editor instance. This constructor should be rarely\n * used, in favor of the <a href=\"#!/api/CKEDITOR\" rel=\"CKEDITOR\" class=\"docClass\">CKEDITOR</a> editor creation functions.\n *\n * @class\n */\n<a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a> = function( ) {\n// ...\n</code></pre>\n\n<p>When you want to reopen the class declaration in another file, use this:</p>\n\n<pre><code>/** @class <a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a> */\n</code></pre>\n\n<h4 id='dev_code_documentation-section-details'>Details</h4>\n\n<p>The order of tags may look strange, but you can remember it thanks to the following description:</p>\n\n<blockquote><p>Since 3.0 there is a private class <code><a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a></code> which extends <code>CKEDITOR.parent</code> and mixins <code><a href=\"#!/api/CKEDITOR.event\" rel=\"CKEDITOR.event\" class=\"docClass\">CKEDITOR.event</a></code> and <code>CKEDITOR.whatever</code>.</p>\n\n<p>It has a private constructor (switched order &mdash; explained later in the tags list) which accepts the following parameters: ...</p>\n\n<p>See also: ...</p></blockquote>\n\n<p>Important tag details:</p>\n\n<ul>\n<li>By default private classes will not be visible in the packages tree.</li>\n<li>A good example of the difference between \"mixins\" and \"extends\" is that <code><a href=\"#!/api/CKEDITOR.event\" rel=\"CKEDITOR.event\" class=\"docClass\">CKEDITOR.event</a></code> is mixed in various other classes and the <code>CKEDITOR.dom.*</code> structure is based on extending parent classes.</li>\n<li><p>A constructor is in fact a separate documentation \"instance\", because it will be listed with methods. Thus, it may have its own <code>@private</code>, but it has to be placed below it, because everything before will be a part of the class description. However, two <code>@private</code> tags in one comment will not be accepted by JSLinter, so in this case the documentation should be split into two comments.</p>\n\n<p>  A constructor can also be declared completely independently from the class, which is useful when <code><a href=\"#!/api/CKEDITOR.tools-method-createClass\" rel=\"CKEDITOR.tools-method-createClass\" class=\"docClass\">CKEDITOR.tools.createClass</a></code> has been used.</p>\n\n<p>  Note that in the first example <code>@see</code> tags will a part of constructor definition, not class. They may therefore also be placed after mixins.</p></li>\n</ul>\n\n\n<h3 id='dev_code_documentation-section-property-and-configuration-variable'>Property and Configuration Variable</h3>\n\n<p>The following is an example of property documentation.</p>\n\n<pre><code>/**\n * A unique identifier of this editor instance.\n *\n * **Note:** It will be originated from the ID or the name\n * attribute of the element, otherwise a name pattern of\n * 'editor{n}' will be used.\n *\n * @private\n * @readonly\n * @static\n * @property {String/Boolean} [complicatedName=default value]\n * @member <a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a>\n * @see CKEDITOR.whatever\n */\nobj[ 'complicated' + 'Name' ] = this.name || genEditorName();\n</code></pre>\n\n<p>A minimal property documentation:</p>\n\n<pre><code>/**\n * Property description (even this may be omitted, but it is better to always describe a property).\n */\nthis.propertyName = 'value';\n</code></pre>\n\n<p>Which will be recognized as <code>@property {String} [propertyName='value']</code> by JSDuck and will be assigned to the class defined earlier in the file.</p>\n\n<p>Partial:</p>\n\n<pre><code>/**\n * Property description.\n *\n * @property {String} [=config.something (value taken from configuration)]\n */\nthis.propertyName = this.config.something;\n</code></pre>\n\n<p>Basic:</p>\n\n<pre><code>/**\n * Property description.\n *\n * @property propertyName\n */\nthis.propertyName = this.config.something;\n</code></pre>\n\n<h4 id='dev_code_documentation-section-details'>Details</h4>\n\n<ul>\n<li>JSDuck type definitions are awesome &mdash; <a href=\"https://github.com/senchalabs/jsduck/wiki/Type-Definitions\">read more about them</a>.</li>\n<li>Property names, types, and default values may be recognized automatically.</li>\n<li>The default value does not have to be JavaScript code, so in the \"Partial\" example JSDuck will print: \"Defaults to: config.something (value taken from configuration)\".</li>\n<li>If a property is not defined below a class definition or if it belongs to a different class, then <code>@member</code> has to be used. Specifying a namespace in <code>@property</code> is not possible.</li>\n</ul>\n\n\n<h4 id='dev_code_documentation-section-configuration-variables'>Configuration Variables</h4>\n\n<p>To define a configuration variable instead of a property:</p>\n\n<ul>\n<li>Use <code>@cfg</code> instead of <code>@property</code>. The format is the same.</li>\n<li><code>@private</code>, <code>@readonly</code>, <code>@static</code> may not work (have not been tested).</li>\n</ul>\n\n\n<h3 id='dev_code_documentation-section-method-and-event'>Method and Event</h3>\n\n<p>The following is an example of method documentation.</p>\n\n<pre><code>/**\n * The <a href=\"#!/api/CKEDITOR.dom.element\" rel=\"CKEDITOR.dom.element\" class=\"docClass\">CKEDITOR.dom.element</a> representing an element. If the\n * element is a native DOM element, it will be transformed into a valid\n * <a href=\"#!/api/CKEDITOR.dom.element\" rel=\"CKEDITOR.dom.element\" class=\"docClass\">CKEDITOR.dom.element</a> object.\n *\n *      var element = new <a href=\"#!/api/CKEDITOR.dom.element\" rel=\"CKEDITOR.dom.element\" class=\"docClass\">CKEDITOR.dom.element</a>( 'span' );\n *      alert( element == <a href=\"#!/api/CKEDITOR.dom.element-static-method-get\" rel=\"CKEDITOR.dom.element-static-method-get\" class=\"docClass\">CKEDITOR.dom.element.get</a>( element ) ); // true\n *\n *      var element = document.getElementById( 'myElement' );\n *      alert( <a href=\"#!/api/CKEDITOR.dom.element-static-method-get\" rel=\"CKEDITOR.dom.element-static-method-get\" class=\"docClass\">CKEDITOR.dom.element.get</a>( element ).getName() ); // (e.g.) 'p'\n *\n * @private\n * @static\n * @method complicatedName\n * @member <a href=\"#!/api/CKEDITOR.editor\" rel=\"CKEDITOR.editor\" class=\"docClass\">CKEDITOR.editor</a>\n * @param {String/Object} element Element's ID or name or a native DOM element.\n * @param {Function} fn Callback.\n * @param {String} fn.firstArg Callback's first argument.\n * @param {Number} fn.secondArg Callback's second argument.\n * @param {String} [name='default value']\n * @returns {<a href=\"#!/api/CKEDITOR.dom.element\" rel=\"CKEDITOR.dom.element\" class=\"docClass\">CKEDITOR.dom.element</a>} The transformed element.\n */\nthis[ 'complicated' + 'Name' ] = (function() {\n    return function() {\n    };\n}());\n</code></pre>\n\n<p>Typical:</p>\n\n<pre><code>/**\n * Method description.\n *\n * @param {String/Object} element Element's ID or name or a native DOM element.\n * @param {String} [name='default value']\n * @returns {<a href=\"#!/api/CKEDITOR.dom.element\" rel=\"CKEDITOR.dom.element\" class=\"docClass\">CKEDITOR.dom.element</a>} The transformed element.\n */\nthis.methodName = function() {\n    // ...\n};\n</code></pre>\n\n<h4 id='dev_code_documentation-section-details'>Details</h4>\n\n<ul>\n<li>The <code>@method</code> tag has to be used when it is not clear that a piece of code is a method (e.g. a closure returning a function was used or a reference to a function defined elsewhere) or when the method's name is not obvious.</li>\n<li>Callback arguments may be defined. Also, if a method returns an object, its properties may be defined too &mdash; <a href=\"https://github.com/senchalabs/jsduck/wiki/@return\">read more</a>.</li>\n<li>Both <code>@return</code> and <code>@returns</code> are accepted, but use the latter one.</li>\n</ul>\n\n\n<h4 id='dev_code_documentation-section-events'>Events</h4>\n\n<p>To define an event instead of a method:</p>\n\n<ul>\n<li>Use <code>@event</code> instead of <code>@method</code> &mdash; usually you will have to provide a name.</li>\n<li><code>@returns</code> is not accepted.</li>\n</ul>\n\n\n<h2 id='dev_code_documentation-section-miscellaneous-rules'>Miscellaneous Rules</h2>\n\n<ul>\n<li>Always leave one blank line between the textual comment and the first tag.</li>\n<li>Separate all blocks (paragraphs, code samples, etc.) with one blank line.</li>\n<li>Code samples are indented with at least two tabs after <code>*</code> and remember &mdash; <strong>no spaces</strong> are to be used.</li>\n<li>Always place a dot (<code>.</code>) at the end of a sentence. A sentence starts with an upper-case letter.</li>\n<li>Always use single quotes for JavaScript strings, but double quotes for cites, irony, etc. in textual comments.</li>\n<li>Cross-reference format for links is: <code>CKEDITOR.name.space#property</code>. If there is more than one event/property/configuration/method with the same name, then prepend <code>cfg-</code>, <code>property-</code>, <code>method-</code> or <code>event-</code> to the name. The namespace may be omitted if it equals to <code>@member</code> or the current class. See the <a href=\"https://github.com/senchalabs/jsduck/wiki/Guide#cross-references\">\"Cross-references\"</a> section in the JSDuck's guide.</li>\n<li>Use real format in default values: <code>CKEDITOR.name.space.property</code>.</li>\n<li>When describing the value returned or alerted in the code sample, wrap only strings in <code>''</code>. All other types (Boolean, numbers, objects, etc.) should be left unwrapped.</li>\n<li>There is no Integer type in JavaScript and constructors' names should be used as types names &mdash; so Boolean, not boolean.</li>\n<li>In textual comments wrap tokens, names from code, JavaScript values, etc. with <code>``</code> for better visibility. Remember to wrap strings with <code>''</code> &mdash; so: <code>This method may return: `'text text'`.</code></li>\n</ul>\n\n","title":"Code Documentation","meta_description":"Documentation standards for CKEditor source code.","meta_keywords":"ckeditor, editor, wysiwyg, documentation, docs, api, code, source, standards, style"});
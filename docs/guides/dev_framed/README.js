Ext.data.JsonP.dev_framed({"guide":"<h1 id='dev_framed-section-classic-editing'>Classic Editing</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_framed-section-creating-a-classic-editor'>Creating a Classic Editor</a></li>\n<li><a href='#!/guide/dev_framed-section-saving-the-editor-data'>Saving the Editor Data</a></li>\n<li><a href='#!/guide/dev_framed-section-client-side-data-handling'>Client-Side Data Handling</a></li>\n<li><a href='#!/guide/dev_framed-section-complete-sample'>Complete Sample</a></li>\n</ol>\n</div>\n\n<p>Classic editing is the most common way to use CKEditor. In this usage scenario the editor is usually represented by a toolbar and an editing area placed in a specific position on the page. Sometimes it is also called \"framed editing\", because in this case the editor creates a temporary <code>&lt;iframe&gt;</code> element for itself.</p>\n\n<p>This method is used in the <a href=\"#!/guide/dev_installation-section-adding-ckeditor-to-your-page\">Quick Start Guide</a> example. To try it out, see also the <a href=\"http://ckeditor.com/demo#standard\">demo on the official CKEditor site</a>.</p>\n\n<p><img src=\"guides/dev_ckeditor_js_load/classic_example.png\" alt=\"Classic editor example\" width=\"572\" height=\"288\"></p>\n\n<p>After <a href=\"#!/guide/dev_ckeditor_js_load\">loading the CKEditor script</a> you will be ready to create your editor instances.</p>\n\n<h2 id='dev_framed-section-creating-a-classic-editor'>Creating a Classic Editor</h2>\n\n<p>In classic editing, CKEditor works just like a <code>&lt;textarea&gt;</code> HTML element on your page. The editor offers a user interface to write, format, and work with rich text in a hassle-free manner, but the same content could be added (though not that easily) through a <code>&lt;textarea&gt;</code> element, requiring the user to type HTML code inside.</p>\n\n<p>As a matter of fact, CKEditor uses the <code>&lt;textarea&gt;</code> element to transfer its data to the server. The <code>&lt;textarea&gt;</code> element is invisible to the end user. In order to create an editor instance, you must first add a <code>&lt;textarea&gt;</code> element to the source code of your HTML page:</p>\n\n<pre><code>&lt;textarea name=\"editor1\" id=\"editor1\"&gt;&amp;lt;p&amp;gt;Initial value.&amp;lt;/p&amp;gt;&lt;/textarea&gt;\n</code></pre>\n\n<p>Note that if you want to load data into the editor, for example from a database, you need to put that data inside the <code>&lt;textarea&gt;</code> element, just like the HTML-encoded <code>&lt;p&gt;</code> element in the example above. In this case the <code>&lt;textarea&gt;</code> element was named <code>editor1</code>. This name can be used in the server-side code later, when receiving the posted data.</p>\n\n<p>After the <code>&lt;textarea&gt;</code> element is inserted, you can use the <a href=\"#!/api\">CKEditor JavaScript API</a> to replace this HTML element with an editor instance. A simple <a href=\"#!/api/CKEDITOR-method-replace\" rel=\"CKEDITOR-method-replace\" class=\"docClass\">CKEDITOR.replace</a> method call will be enough to achieve that:</p>\n\n<pre><code>&lt;script&gt;\n    <a href=\"#!/api/CKEDITOR-method-replace\" rel=\"CKEDITOR-method-replace\" class=\"docClass\">CKEDITOR.replace</a>( 'editor1' );\n&lt;/script&gt;\n</code></pre>\n\n<p>This script block must be included at any point after the <code>&lt;textarea&gt;</code> tag in the source code of the page. You can also call the <code><a href=\"#!/api/CKEDITOR-method-replace\" rel=\"CKEDITOR-method-replace\" class=\"docClass\">CKEDITOR.replace</a></code> method inside the <code>&lt;head&gt;</code> section, but in this case you will need to listen for the <code>window.onload</code> event:</p>\n\n<pre><code>&lt;script&gt;\n    window.onload = function() {\n        <a href=\"#!/api/CKEDITOR-method-replace\" rel=\"CKEDITOR-method-replace\" class=\"docClass\">CKEDITOR.replace</a>( 'editor1' );\n    };\n&lt;/script&gt;\n</code></pre>\n\n<h2 id='dev_framed-section-saving-the-editor-data'>Saving the Editor Data</h2>\n\n<p>As stated above, the editor works just like a <code>&lt;textarea&gt;</code> field. This means that when submitting a form containing an editor instance, its data will simply be posted, using the <code>&lt;textarea&gt;</code> element name as the key to retrieve it.</p>\n\n<p>For example, following the above code sample, you could create this PHP code:</p>\n\n<pre><code>&lt;?php\n    $editor_data = $_POST[ 'editor1' ];\n?&gt;\n</code></pre>\n\n<h2 id='dev_framed-section-client-side-data-handling'>Client-Side Data Handling</h2>\n\n<p>Some applications (like those based on Ajax) need to handle all data on the client side, sending it to the server using their specific methods. If this is the case, it is enough to use the <a href=\"#!/api\">CKEditor JavaScript API</a> to easily retrieve the editor instance data. In order to do this, you can use the <a href=\"#!/api/CKEDITOR.editor-method-getData\" rel=\"CKEDITOR.editor-method-getData\" class=\"docClass\">getData</a> method:</p>\n\n<pre><code>&lt;script&gt;\n    var editor_data = CKEDITOR.instances.editor1.getData();\n&lt;/script&gt;\n</code></pre>\n\n<h2 id='dev_framed-section-complete-sample'>Complete Sample</h2>\n\n<p>To insert a CKEditor instance, you can use the following sample that creates a basic page containing a form with a <code>&lt;textarea&gt;</code> element that is replaced with CKEditor.</p>\n\n<pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n    &lt;title&gt;CKEditor Classic Editing Sample&lt;/title&gt;\n    &lt;!-- Make sure the path to CKEditor is correct. --&gt;\n    &lt;script src=\"/ckeditor/ckeditor.js\"&gt;&lt;/script&gt;\n&lt;/head&gt;\n&lt;body&gt;\n    &lt;form method=\"post\"&gt;\n        &lt;p&gt;\n            My Editor:&lt;br&gt;\n            &lt;textarea name=\"editor1\" id=\"editor1\"&gt;&amp;lt;p&amp;gt;Initial value.&amp;lt;/p&amp;gt;&lt;/textarea&gt;\n            &lt;script&gt;\n                <a href=\"#!/api/CKEDITOR-method-replace\" rel=\"CKEDITOR-method-replace\" class=\"docClass\">CKEDITOR.replace</a>( 'editor1' );\n            &lt;/script&gt;\n        &lt;/p&gt;\n        &lt;p&gt;\n            &lt;input type=\"submit\"&gt;\n        &lt;/p&gt;\n    &lt;/form&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre>\n","title":"Classic Editing","meta_description":"What classic editing is and how to enable it.","meta_keywords":"ckeditor, editor, wysiwyg, classic, framed, iframe, textarea"});
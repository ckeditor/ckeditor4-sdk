Ext.data.JsonP.dev_readonly({"guide":"<h1 id='dev_readonly-section-read-only-mode'>Read-Only Mode</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_readonly-section-read-only-mode-on-startup'>Read-Only Mode on Startup</a></li>\n<li><a href='#!/guide/dev_readonly-section-read-only-mode-demo'>Read-Only Mode Demo</a></li>\n</ol>\n</div>\n\n<p>CKEditor API includes a few features that make it possible to <strong>render the editor content read-only</strong> (and thus impossible for the user to edit). At the same time some editor features that will not cause the user to modify the content will still be available. This includes, for example, the view of the content source code or features such as preview or editor interface maximization within the browser window.</p>\n\n<p>The most crucial element of the API is the <a href=\"#!/api/CKEDITOR.editor-method-setReadOnly\">setReadOnly()</a> method which puts the editor into the read-only state and restores it to the editable state.</p>\n\n<p>When the editor goes from the editable into the read-only state, its <a href=\"#!/api/CKEDITOR.editor-property-readOnly\">readOnly</a> property is being set to <code>true</code> (for read-only mode) or <code>false</code> (for editable mode). The <a href=\"#!/api/CKEDITOR.editor-event-readOnly\">readOnly</a> event is also fired after each change of the <code>readOnly</code> property.</p>\n\n<p>This is what CKEditor looks like when it is in read-only mode.</p>\n\n<p><p><img src=\"guides/dev_readonly/readonly_01.png\" alt=\"\" width=\"925\" height=\"313\"></p></p>\n\n<h2 id='dev_readonly-section-read-only-mode-on-startup'>Read-Only Mode on Startup</h2>\n\n<p>CKEditor can be configured to open in the read-only mode on startup by setting the <a href=\"#!/api/CKEDITOR.config-cfg-readOnly\">config.readOnly</a> configuration option to <code>true</code>. For example:</p>\n\n<pre><code>config.readOnly = true;\n</code></pre>\n\n<p>The same can be achieved by setting the <code>disabled</code> attribute for the <code>&lt;textarea&gt;</code> element that CKEditor replaces.</p>\n\n<h2 id='dev_readonly-section-read-only-mode-demo'>Read-Only Mode Demo</h2>\n\n<p>See also the <a href=\"../samples/readonly.html\">working Read-Only Mode sample</a> that showcases toggling between the read-only and editable mode.</p>\n","title":"Read-Only Mode","meta_description":"Using CKEditor Read-Only Mode.","meta_keywords":"ckeditor, editor, wysiwyg, readonly, read-only, uneditable, noneditable, ui, interface, configure, configuration, setup, settings, options, customization, customize, customise, customisation, config, modification, modify, change"});